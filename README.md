# 🧩 Кейс: «Категории, которые исчезали»

## 🎭 Контекст истории

Компания **«ХвостСервис»** выкатывает новый релиз. Один из микросервисов — `task-service`, который отдаёт категории задач для фронтенда.

Разработчик **Игорь** добавил загрузку справочника категорий из файла при старте сервиса. Загрузка занимает ~30 секунд.

После загрузки сервис отвечает корректно.

Однако в момент деплоя фронтенд иногда начинает получать **пустой список категорий**. Через ~30 секунд данные появляются сами, но к этому моменту уже:
- пользователи пугаются,
- аналитики говорят «категорий нет»,
- менеджеры задают вопросы.

Тимлид поручает Игорю разобраться.

---

## Наблюдения

- Деплой выполняется через **rolling update** (почитать, что это такое) с 2 репликами.
- На старте сервис поднимает HTTP-сервер и загружает файл с категориями.
- Пользватели в течении 30 секунд видят пустоту на экране
- На стенде видно, что новый Pod **иногда получает трафик до загрузки файла**.
- В коде есть флаг `termination` (назначение неочевидно).
- Нет возможности вывести под из балансировки вручную без перезапуска, поэтому отладка вызывает боль
---

## Требования тимлида

> «Нужно найти место загрузки файла. Pod не должен считаться готовым, пока не загрузил данные, и не должен отдавать пустые категории на старте»

> «Реализовать механизм, который позволит сказать окрестратору, что pod временно не будет обслуживать трафик. Взгляни на механику cordon/uncoedon в кубе. 
> Вот референс https://cast.ai/blog/kubernetes-cordon-how-it-works-and-when-to-use-it.
> Это нужно нам для исследований и раскопок во время проблем, пригодится»
---

### Задача 1 — Пока справочник категорий не загружен → Pod **не готов**.

Выбрать нужный тип пробы и реализовать в ней логику, позволяющую понять оркестатору, что севрис *task-service* в боевой готовности и категории успешно загружены

Убедиться в наличии проблемы можно лично запустив стенд и дернув ручку получения категорий `/v1/categories`

**Ожидание:** новый Pod не участвует в балансировке раньше времени.

---

### Задача 2 — Изучить код приложения *task-service* и ответить на вопросы

Флаг `terminated int32` в структуре `App`:
- зачем он вообще существует?
- что он сигналит?
- как используется при graceful shutdown?
- имеет ли смысл он вообще?

**Ожидание:** осознанное решение, а не «историческое наследие».

---
### Задача 3 — Научить *task-service* сообщать оркестратору о намерении выйти из балансировки нагрузки 

Бывший коллега Петя начал делать данную задачу, однако покинул коллектив и теперь вам предстоит закончить начатое.

Он успел зарегистрировать в `http` сервере пару ручек, но реализацию сделать, увы, не успел.

```go
a.adminMux.Post("/cordon", func(writer http.ResponseWriter, request *http.Request) {
// TODO: как я могу вывести мой под из балансировки тут? Что делать?
})
a.adminMux.Post("/uncordon", func(writer http.ResponseWriter, request *http.Request) {
// TODO: как я могу ввести мой под в балансировку тут? Что делать?
})
```

От вас требуется реализовать их таким образом, чтобы k8s, придя к вам по пути healthcheck/readiness, понял, что обслуживать трафик вы либо готовы, либо нет.

**Ожидание:** можно вручную вывести Pod из балансировки и вернуть обратно.

## Технические ожидания

После выполнения решения:

1. Контейнеры `task-service` считаются ready только после загрузки файла с категориями
- контейнер перейдет из статуса `unhealthy` -> `healthy` ~ через 30сек (после загрузки файла)
- не будет перезапущен
- введя команду `docker ps` после первой итерации *health check* статус контейнера будет unhealthy, а через 30 сек он сменится на `healthy`
- так же `autoheal` будет видеть контейнер в промежуточном состоянии и не будет перезапускать его

2. Появилась возможность вручную (или же автоматикой) выводить контейнеры из балансировки
- вызовы ручек `/cordon` + `/uncordon` переключают состояние контейнера (healthy / unhealthy)
- введя команду `docker ps` после переключения и наступления итерации *health check* статус контейнера будет меняться (будет перезапущен)

---
## Критерии успеха

- **Функционально**: пользователи не видят пустых категорий
- **Операционно**: есть механизм «поднять → прогреть → включить в трафик»
- **Инженерно**: понятна роль `termination` и lifecycle сигналов
- **Kubernetes-логически**: готовность Pod отражает фактическую готовность
---


## 🧪 Как убедиться, что код делает ровно то, что от него требуется?

Перед тем как разбираться с probes и graceful shutdown, важно понимать,
**как именно Docker Compose определяет состояние контейнера**  
и почему иногда «всё сломалось», хотя контейнер всё ещё работает.

Ниже — разбор `healthcheck` и роли `autoheal`.

---

## 🩺 Как работает `healthcheck` в Docker Compose

По умолчанию каждый контейнер после запуска находится в статусе: `starting`


Далее в работу вступает секция `healthcheck`.
---

### 🎯 Что делает `healthcheck`

`healthcheck` решает **одну простую задачу**:

- опросить контейнер на предмет *живости*
- если контейнер жив → статус становится `healthy`
- если контейнер не жив → статус становится `unhealthy`

И всё.  
`healthcheck` **не управляет трафиком, не рестартит контейнер и не чинит ошибки**.

> Healthcheck постоянно прашивает контейнер, точно так же, как и оркестратор, который постоянно опрашивает наш контейнер в production.
---

## 🔁 Как происходит проверка на живость

### ⏳ `start_period: 10s`

- healthcheck **начинает выполняться сразу**
- но **неудачные попытки не учитываются** в течение первых `10 секунд`
- это «поблажка» контейнеру, чтобы он успел запуститься

---

### 🔄 `retries: 5`

- количество **подряд идущих неудачных попыток**
- после которых контейнер считается `unhealthy`

---

### ⏱ `timeout: 4s`

- максимальное время ожидания **одной** попытки
- если команда не уложилась — попытка считается неуспешной

---

### ⏲ `interval: 5s`

- время **между попытками** выполнения healthcheck

---

### 🧪 `test`

- команда, которая выполняется **внутри контейнера**
- Docker смотрит **только на exit code**

| Exit code | Результат |
|---|---|
| `0` | попытка успешна |
| `!= 0` | попытка неуспешна |

stdout / stderr **не анализируются**.

---

> В нашем случае: у healthcheck'а есть 10 + 5*5 = 35s на выполнение проверок. 
Этого хватет для того, чтобы файлик был загружен.


## 🚨 Важный момент: `unhealthy` — это тупик

Если контейнер перешёл в статус: `unhealthy`


то:

- Docker Compose **не перезапустит его автоматически**
- контейнер продолжает работать, но помечен как нездоровый
- сам по себе он **не вернётся в `healthy`**

---

## 🔧 Для этого нужен `autoheal`

### 🧩 Что делает `autoheal`

`autoheal` — это отдельный контейнер-наблюдатель, который:

- следит за контейнерами с лейблом:

```yaml
labels:
  - autoheal=true
```

- если контейнер с этим лейблом перешёл в unhealthy
- перезапускает его

## Как посмотреть, что происходит с контейнером

### 1. `docker ps`

Команда `docker ps` показывает сводную информацию по всем запущенным контейнерам:

- список контейнеров
- текущий статус контейнера (`starting`, `healthy`, `unhealthy`)
- время работы (uptime)
- идентификатор контейнера

Эта команда удобна для быстрого обзора состояния системы.

---

### 2. `docker inspect <container_id>`

Команда `docker inspect` позволяет получить подробную информацию о конкретном контейнере.

С её помощью можно увидеть детали, связанные с `healthcheck`:

- текущий health-статус контейнера
- количество подряд неуспешных проверок
- значение `failstreak`
- историю выполнений healthcheck-команд

Команда особенно полезна для отладки проблем со стартом контейнера и некорректной работой `healthcheck`.

---


## Поведение контейнера при `cordon / uncordon` в Docker Compose

В Docker Compose нет управления трафиком, как в Kubernetes.

Поэтому в рамках данного задания `cordon / uncordon` реализуется **на уровне приложения**
через изменение поведения `healthcheck`.

---

## Что означает `cordon`

При переводе контейнера в состояние **cordon** приложение должно:

- ❌ перестать считаться *здоровым*
- ❌ сигнализировать внешней системе, что контейнер **не готов принимать новые запросы**
- ✅ продолжать работать и обрабатывать уже начатые операции
- ✅ оставаться запущенным и доступным для отладки

Технически это реализуется так:

- `healthcheck` начинает **возвращать ошибку** (`exit code != 0`)
- контейнер переходит в статус `unhealthy` ТОЛЬКО ПО ДОСТИЖЕНИЮ КОЛИЧЕСТА RETRY'ев, которое мы указали в compose файле
- Docker Compose помечает контейнер как нездоровый

---

## Что означает `uncordon`

При переходе в состояние **uncordon** приложение должно:

- снова успешно проходить `healthcheck`
- вернуть `exit code = 0`
- перейти в статус `healthy`

Контейнер **не перезапускается**, состояние меняется динамически.

Тут можно поставить побольше попыток, чтобы мы отловили контейнер в статусе starting и могли его перевести в `uncordon`.

---

## Важное ограничение Docker Compose

В отличие от Kubernetes:

- Docker Compose **не исключает контейнер из балансировки автоматически**
- Docker Compose **не управляет трафиком**
- статус `unhealthy` — это **информационный сигнал**

Поэтому:
- реальный контроль трафика происходит **в приложении или на клиенте**
- healthcheck используется как **индикатор состояния**, а не как маршрутизатор

---

## Роль `autoheal`

Так как Docker Compose **не умеет восстанавливать контейнеры** из `unhealthy`:

- используется контейнер `autoheal`
- он отслеживает контейнеры с лейблом:

```yaml
labels:
  - autoheal=true

